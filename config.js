// Cambiar este valor al host del backend (Cloudflared tunnel o IP local).
// Ej: "https://api.tu-tunnel.trycloudflare.com" o "http://127.0.0.1:5000".
window.B24_API_BASE = "http://127.0.0.1:5000";

// Polling interval en milisegundos (default 60s).
window.B24_POLL_MS = 60000;

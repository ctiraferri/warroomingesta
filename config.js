// Backend del b24_api expuesto via Cloudflared tunnel (HTTPS).
// Para dev local: cambiar temporalmente a "http://127.0.0.1:5000".
window.B24_API_BASE = "https://warroomdigital.com.ar";

// Polling interval en milisegundos (default 60s).
window.B24_POLL_MS = 60000;
